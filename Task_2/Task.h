#ifndef TASK_H
#define TASK_H

#include "MemoryManager.h"

/**
 *	Processing one query and output (if necessary) its result.
 */
extern void executeQuery(MemoryManager &memory_manager);

#endif // !TASK_H